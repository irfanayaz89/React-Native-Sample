import { StyleSheet } from 'react-native';
const Styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
    },
    keyStyle: {
        flex: 0.3,
        fontWeight: 'bold',
        color: 'white',
    },
    valueStyle: {
        flex: 0.8,
        color: 'white',
        flexWrap: 'wrap'
    },
})

export default Styles